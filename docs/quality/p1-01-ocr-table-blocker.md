# P1-01 `ocr_table` 语料阻塞报告

- 日期：2026-08-23
- 分支：`feat/P1-01-golden-v1`
- 状态：停线，等待语料补充或规格裁决
- 触发条件：Golden Set 标注所需语料内容缺失，影响出题

## 结论

当前仓库中的 `机器学习基础.pdf` 与 `Git命令手册.docx` 均不包含表格或图片上下文，无法按 P1-01 任务要求真实产出 15 条 `ocr_table` 题。继续生成会把普通段落伪装成表格/图片证据，因此未创建正式 `v1.jsonl`，也未启动 T2–T5。

## 复核证据

### `test_docs/机器学习基础.pdf`

- SHA-256：`8c929e4471e3dc1faa6b503e277c2eb56d521ed4012506038a0a538d3c86c110`
- 共 1 页。
- `pypdf` 检测到该页图片对象数为 0。
- 页面可视化复核显示内容只有标题、普通段落、编号列表和项目符号列表；没有表格、图表、截图或扫描图片。
- 文本层可完整抽取，不属于需要 OCR 才能读取的扫描页。

### `test_docs/Git命令手册.docx`

- SHA-256：`3b5d9ceb06e08e13b5eed0ae1c7fcbb43b86a5ce91d3c0ac034e9e5c1d3a2b6f`
- `python-docx` 检测结果：29 个非空段落、0 个表格、0 个内联图片。
- DOCX 压缩包内没有 `word/media/`、`word/embeddings/` 或 drawing XML。
- 文档内容由标题、小节标题和 Git 命令普通段落构成。

## 对配额的影响

现有 56 条草稿的类型分布为：

| 类型 | 已有 | 正式目标 | 缺口 |
|------|-----:|---------:|-----:|
| `factoid` | 25 | 55 | 30 |
| `multi_span` | 10 | 15 | 5 |
| `exact_term` | 13 | 15 | 2 |
| `ocr_table` | 0 | 15 | 15 |
| `unanswerable` | 8 | 20 | 12 |

其余 49 条缺口可从现有 21 份语料中继续标注，但 `ocr_table` 的 15 条硬配额没有符合指定来源和 locator 要求的事实依据。

## 未执行事项

- 未生成或提交 `backend/evals/golden_set/v1.jsonl`。
- 未将普通文本题错误标注为 `ocr_table`。
- 未提交 commit、未推送分支、未创建或合并 PR。
- 未启动基线、迁移、域契约或统一融合任务。

## 解除阻塞所需裁决

任选其一：

1. 替换或补充含真实表格、图表、截图或扫描图片的 PDF/DOCX 语料，并明确这些文件仍属于 Golden Set 的 21 份受控语料；建议至少提供足以支撑 15 个不重复问题的上下文。
2. 明确批准将 PDF/DOCX 的版面化普通文本（标题、编号列表、项目符号列表）计入 `ocr_table`，并同步修改 P1-01 规格中“表格和图片上下文”的定义。
3. 调整 `ocr_table` 配额或允许从其他受控语料引入真实表格/图片上下文，同时给出新的精确配额与 locator 规则。

收到裁决后，应继续使用当前分支完成 T1；T1 合并且五项 CI 门禁全绿后，方可依次启动后续任务。

## 已裁决：方案一

- 裁决日期：2026-08-23
- 裁决角色：Integrator
- 状态：原语料缺失阻塞已解除，报告保留为质量审计记录

Integrator 确认原停线判断与文件证据正确，并决定补充真实原生表格语料，保持 `ocr_table` 题型名与 15 条配额不变。已新增幂等生成脚本 `backend/scripts/gen_table_corpus.py`，生成以下三份受控语料：

1. `HTTP状态码速查表.docx`：3 张原生表格，分别覆盖 2xx、4xx、5xx。
2. `Git常用命令对照表.xlsx`：2 个工作表，分别覆盖基础操作、分支与远程。
3. `Linux文本处理三剑客.csv`：15 条 grep、sed、awk 用法。

语料总数由 21 增至 24。Phase 1 的 `ocr_table` 定义澄清为依据 DOCX/XLSX/CSV/PDF 原生数字表格出题；扫描件和图片 OCR 样本延后到 Phase 2。新增语料的结构与版式已验证，生产解析链路的 XLSX 依赖缺失与 Windows CSV 编码问题另在 `backend/evals/golden_set/REVIEW-STATUS.md` 如实记录。
