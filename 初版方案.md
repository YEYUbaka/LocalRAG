这份方案专为技术评审设计，涵盖了从架构理念、技术选型、核心链路到工程化落地的全生命周期规划。内容结构严谨，可直接作为项目立项或技术评审的汇报材料。

------

# 本地个人知识库系统（RAG）设计与开发方案

## 一、 项目背景与目标

在信息过载时代，个人知识管理面临“存而不用、用而难找”的痛点。本项目旨在构建一套基于检索增强生成（RAG）技术的本地个人知识库系统。通过引入大语言模型（LLM），将传统的“关键词搜索”升级为“语义问答”，帮助用户高效学习、深度理解并沉淀个人知识。

## 二、 核心架构设计

本系统采用**“本地应用服务 + 混合模型推理”**的混合架构，在保障数据隐私的前提下，实现极致的性价比与系统性能。

### 2.1 架构分层

1. **前端交互层 (TypeScript)**：负责文档上传、对话交互、引用溯源展示。

2. **后端服务层 (Python/FastAPI)**：负责业务逻辑编排、异步任务处理、API 网关。

3. **AI 编排层 (LangChain)**：封装文档解析、文本分块、向量检索、Prompt 组装等 RAG 核心链路。

4. 数据与模型层

   ：

   - **向量存储**：ChromaDB（本地持久化）。
   - **Embedding 模型**：本地轻量级模型（如 `bge-small-zh-v1.5`），负责文本向量化，保障隐私与零成本。
   - **LLM 推理**：云端大模型 API（如 Qwen-Max/GPT-4o），负责复杂逻辑推理与总结。

### 2.2 核心优势

- **数据安全**：原始文档与向量索引完全本地化，仅将脱敏后的检索片段发送至云端。
- **成本最优**：高频的 Embedding 计算在本地 CPU 免费完成，仅在用户提问时按需消耗云端 Token。
- **性能卓越**：本地 Embedding 毫秒级响应，云端 LLM 提供顶级智商上限。

## 三、 核心技术栈选型

| 模块           | 技术选型               | 选型理由                                            |
| -------------- | ---------------------- | --------------------------------------------------- |
| 后端语言       | Python 3.10+           | AI 生态最完善，LangChain 原生支持                   |
| 前端语言       | TypeScript + React     | 类型安全，适合构建复杂的现代 Web 交互界面           |
| AI 编排框架    | LangChain              | 业界标准，提供开箱即用的 RAG 链路封装               |
| 向量数据库     | ChromaDB               | 纯 Python 编写，支持本地持久化，轻量且易于集成      |
| 本地 Embedding | BAAI/bge-small-zh-v1.5 | 中文语义理解极佳，体积小（~90MB），CPU 即可流畅运行 |
| 云端 LLM       | 通义千问 / OpenAI API  | 逻辑推理能力强，支持长上下文，API 兼容性好          |

## 四、 核心业务流程与链路设计

### 4.1 知识入库链路（离线处理）

1. **文档解析**：支持 PDF、Word、Markdown 等格式，提取纯文本与元数据（作者、时间、页码）。
2. **智能分块 (Chunking)**：采用 `RecursiveCharacterTextSplitter`，按段落、句子等自然语义分割。推荐 `chunk_size=500`, `chunk_overlap=50`，兼顾检索精度与上下文连贯性。
3. **本地向量化**：调用本地 `bge` 模型将文本块转化为向量。
4. **持久化存储**：将向量、原文及元数据写入本地 ChromaDB。

### 4.2 问答检索链路（在线实时）

1. **问题向量化**：用户提问后，本地模型实时将问题转为向量。
2. **语义检索**：在 ChromaDB 中进行余弦相似度匹配，召回 Top-K（建议 3~5 个）最相关片段。
3. **Prompt 组装**：将检索到的片段作为 Context，结合用户问题与系统预设指令组装 Prompt。
4. **云端生成**：调用云端 LLM 生成最终答案，并返回引用的文档来源。

## 五、 工程化落地与进阶策略

### 5.1 异步与并发处理

由于文档入库涉及大量本地 CPU 计算，且问答依赖外部 API，后端必须采用异步架构（FastAPI + asyncio）。对于大批量文档上传，建议引入后台任务队列（如 Celery），避免阻塞主线程。

### 5.2 增量更新机制

个人知识库是动态生长的。系统需支持增量索引，通过文件 MD5 哈希比对，仅对新增或修改的文档进行重新分块与向量化，大幅降低重复计算开销。

### 5.3 容错与降级设计

- **API 容错**：针对云端 LLM 的网络波动或限流，设计重试机制与超时熔断。
- **降级策略**：预留本地小模型（如 Qwen-1.8B）作为备用。当云端 API 不可用时，自动切换至本地模型，确保系统“可用”（牺牲部分回答质量换取服务连续性）。

### 5.4 检索质量优化（进阶）

- **混合检索 (Hybrid Search)**：未来可引入 BM25 关键词检索与向量检索结合，解决专有名词检索不准的问题。
- **重排序 (Rerank)**：在召回 Top-K 后，增加一个轻量级 Cross-Encoder 模型进行二次打分排序，将最精准的内容喂给 LLM。

## 六、 项目实施计划（MVP 阶段）

1. **Week 1**：完成 Python 后端搭建，跑通“文档解析 -> 本地 Embedding -> ChromaDB 存储”的入库链路。
2. **Week 2**：集成云端 LLM API，完成 RAG 问答链路的调试与 Prompt 调优。
3. **Week 3**：开发 TypeScript 前端界面，实现文档上传、对话流式输出与引用溯源。
4. **Week 4**：系统联调、性能压测与 Bug 修复，输出 MVP 版本进行内部体验。

## 七、 风险评估与应对

- **检索幻觉**：大模型可能基于不相关的上下文强行回答。**应对**：在 Prompt 中严格限制“仅根据提供的上下文回答，若无相关信息请明确告知”。
- **长文档处理慢**：首次导入大量文档时 CPU 占用高。**应对**：限制单次上传文件大小，后台异步处理并提供进度反馈。
- **API 成本失控**：用户频繁提问导致 Token 消耗过快。**应对**：严格控制检索的 `Top-K` 数量，并在前端增加防抖与提问频率限制。

------

**评审建议**：本方案在技术可行性、成本控制和数据隐私之间取得了良好的平衡。建议评审通过后，优先启动 MVP 阶段的后端核心链路验证，以最低成本验证 RAG 效果。

